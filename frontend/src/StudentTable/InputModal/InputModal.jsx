import React from 'react';
import ModalFrame from "./ModalFrame";

const InputModal = ({modalToggle, handleModalToggle}) => {
    return (
        <ModalFrame modalToggle={modalToggle} handleModalToggle={handleModalToggle}>
            hier weiter machen
        </ModalFrame>
    )
}

export default InputModal;
